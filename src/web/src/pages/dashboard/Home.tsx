const DashboardHome = () => {
    return (
        <div className="animate-in fade-in duration-500">
            {/* Minimal Dashboard Home - Content removed as requested */}
        </div>
    );
};

export default DashboardHome;
